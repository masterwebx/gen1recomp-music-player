# Music Player

SELECT+A opens the in-game music list. START toggles favorites. SELECT+START plays the favorites queue. SELECT in the list restores map music.

## Install

Drop into `mods/` and enable. Needs `engine_internals`.

## License

MIT
